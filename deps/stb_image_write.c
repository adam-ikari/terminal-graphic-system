/*
 * stb_image_write — stb single-header library implementation unit.
 * Vendored (upstream v1.16); the libsixel config.h shim is not needed on
 * any platform TGS targets (C99 memcpy/memmove are always present).
 */
#define STB_IMAGE_WRITE_IMPLEMENTATION
#include "stb_image_write.h"

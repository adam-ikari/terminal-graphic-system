---
title: 简介
sidebar_position: 1
---

# TGS 是什么

**TGS（Terminal Graphic System）是运行在终端里的图形 API**：程序 ⇄ 渲染器点对点，一程序一画布。协议只回答两个问题——**画什么**（绘制原语）与**输入怎么来**（事件）。

像 X11、OpenGL、Wayland 一样，TGS **不提供控件、不提供布局、不提供焦点模型**：那些是程序侧 toolkit 的职责。

:::info TGS ⊃ kitty
帧承载于 kitty 图形协议的 APC 通道（`ESC _ … ESC \`）。kitty 原生像素帧与 TGS 帧（`G1;…`）共享同一通道：TGS 在能力上包含 kitty（像素呈现），并扩展原语语义、事件与 IME。详见 [kitty 兼容面](./kitty-compat)。
:::

## 定案：五条第一性决策

| 决策 | 内容 |
|------|------|
| **协议 = 绘制语义** | 线上只有 3 个绘制原语 + 事件；无控件目录、无 win_id、无焦点模型 |
| **无窗口管理** | 合成器不管理窗口 / 激活 / z-order——那是外层 WM 程序的事；协议无 win_id |
| **无焦点模型** | 控件级焦点完全程序侧；窗口级键盘路由是合成器机制 |
| **IME 独立程序** | 候选窗由 IME 程序自己绘制；TGS 只承载输入变换管道 |
| **TGS ⊃ kitty** | 帧走 kitty G APC 通道（`G1` 子命名空间）；呈现层用 kitty 图形序列 |

**第一性判据**：协议只承载**机制**（绘制、命中、输入采集、IME 管道）；一切**策略**（布局、控件、焦点、文本缓冲、值状态）在程序侧。

## 关键数字

来自 [验证报告](./verification)（与仓库 `docs/VERIFY-REPORT.html` 同源，2026-09-24）：

- **ctest 67/67** 全绿
- 像素级往返 **120000/120000 = 100%，maxdiff=0**（z 序遮挡 vs ground truth）
- 脏区传输空闲 **0 B/s**
- 锚点节拍 **123.7fps @120Hz**、**245.3fps @240 门控**（上限 250）
- 全屏翻页 **109.9fps**——如实标注的**诚实上限**（真实每帧工作量 ≈9.1ms 超 8ms 预算）

## C99，一个外部依赖

协议实现是 C99；唯一外部依赖是系统 zlib（PNG 编码，级 1，约 7ms/帧 800×600）。接收 kitty 原生 PNG 帧用 vendored stb_image（仅 PNG）。

## 从哪读起

- 要跑起来 → [快速开始](./getting-started)
- 要理解分层 → [架构](./architecture)
- 要看线上格式 → [协议规范](./protocol)
- 要确认数字怎么来的 → [验证报告](./verification)

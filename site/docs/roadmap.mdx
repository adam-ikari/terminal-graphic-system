---
title: 路线
sidebar_position: 7
---

# 路线

## 当前在列

1. **程序侧控件库（libtgs-ui）**——图形语义价值闭环：radio 互斥组、list 组合、程序侧焦点管理、diff / 重排。在 L0 的绘制原语之上给程序一个组合层。方法论（规范级）：**组合过程中每个摩擦点都是协议缺口的真实信号**——按钮退役后 = CONTAINER + LABEL + CLICK 的组合逻辑，就该沉淀在这个库里。

## 已收口

| 项 | 结论 |
|----|------|
| **kitty 原生 G 帧落地** | ✅ 完成（2026-09-24）——解码 / 放置 / ACK / z 序，ctest 67/67，遮挡 120000/120000。见[验证报告](./verification) |
| **全屏翻页收口（109.9 → 120）** | ❌ 收口为**诚实上限**（决定不再优化）——真实每帧 ≈9.1ms ＞ 8ms 预算的事实与后续路径记录在原报告 §6 |
| **真 kitty 终端实测** | ❌ **环境不可行**——本机无 kitty / ghostty / wezterm、无图形显示；字节流 + PIL 权威回放即现有实测面 |

## 分层愿景

L0 → L6 的完整窗口系统路线见[架构 · 分层路线](./architecture#分层路线-l0--l6)。当前基线：**L0 已落地并稳定**。
